# Implementation-agent preamble
# Lines above the first body line (this comment block) are stripped before the
# agent sees anything — keep human notes up here. The body below is the ONLY
# environment framing every run receives, identical across all scenarios. Keep
# it to the floor: any hint about HOW to navigate the MCP would mask the doc
# deficiencies the arena exists to find.

idealyst MCP available, use it.
