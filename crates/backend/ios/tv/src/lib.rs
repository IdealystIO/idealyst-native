//! tvOS backend (skeleton).
//!
//! Mirrors `backend-ios-mobile` in shape but targets tvOS. The
//! touch-input layer is replaced with the focus engine + remote
//! input; layout and rendering come from `backend-ios-core`.
//!
//! Empty until the focus + TVUIKit work lands.
