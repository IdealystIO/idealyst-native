[package]
name = "robot-mcp-proxy"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "MCP server (stdio) that connects to a running app's robot bridge over TCP"

[[bin]]
name = "robot-mcp-proxy"
path = "src/main.rs"

[dependencies]
serde_json = "1"
