[package]
name = "reactive-arena"
version.workspace = true
edition.workspace = true
license.workspace = true
# Not published to the idealyst registry: this crate is workspace-internal
# (tooling, demo, smoke test, or benchmark). Consumers never name it.
publish = false

# Prototype crate validating Copy-handle reactive primitives via a thread-local
# arena. Not integrated with runtime-core; lives here so we can prove the
# design works before committing to a framework-wide refactor.
