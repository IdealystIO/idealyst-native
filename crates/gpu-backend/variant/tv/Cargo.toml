[package]
name = "variant-tv"
version.workspace = true
edition.workspace = true
license.workspace = true

# TV-sized preview variant.

[dependencies]
host-winit = { workspace = true }
render-wgpu = { workspace = true }
# Style/host vocabulary (`ColorScheme`). `runtime-core` is NOT a
# dependency: this variant has no `Element`/`Backend` surface left.
runtime-shared = { workspace = true }

[features]
default = []
runtime-server = ["host-winit/runtime-server"]
