query GetBooks {
  books {
    id
    title
    author
  }
}

query GetBook($id: ID!) {
  book(id: $id) {
    id
    title
    author
  }
}

query GetFail {
  fail
}

mutation AddBook($title: String!, $author: String!) {
  addBook(title: $title, author: $author) {
    id
    title
    author
  }
}
