//! Web-backend handler stub for Drawer navigator. **Not yet wired** —
//! see `stack-navigator/src/web.rs` for the port checklist; analog
//! applies here against `backend-web/src/primitives/navigator.rs`'s
//! `create_drawer_navigator` impl (scrim + sidebar overlay).

use backend_web::WebBackend;

pub fn register(_backend: &mut WebBackend) {}
