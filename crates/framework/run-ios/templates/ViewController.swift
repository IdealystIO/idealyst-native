// GENERATED by `idealyst run ios`. Hands the root UIView to the Rust
// wrapper's `ios_main` so the framework can mount under it.

import UIKit

class ViewController: UIViewController {
    override func viewDidLoad() {
        super.viewDidLoad()
        view.backgroundColor = .white

        let rawPtr = Unmanaged.passUnretained(view).toOpaque()
        ios_main(rawPtr)
    }

    deinit {
        ios_teardown()
    }
}
